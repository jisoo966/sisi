"use client";

import { MemoryTrail } from "@/components/sisi/moments/MemoryTrail";

export const dynamic = "force-dynamic";

/** Kept for reference: the earlier vertical Memory Trail. */
export default function VerticalTrailPage() {
  return <MemoryTrail />;
}
